1.取消cin,cout的时间

ios::sync_with_stdio(false);
cin.tie(nullptr);
cout.tie(nullptr);



2.宏定义

#define ll long long
#define x first
#define y second


3.链表
struct ListNode {
    int data;
    ListNode *prev;
    ListNode *next;
    ListNode(int x) : data(x), prev(nullptr), next(nullptr) {}单独创建一个链表节点
};

for循环的使用可以是for (ListNode *p = head; p != nullptr; p = p->next)

当用ListNode* L = new ListNode(0);初始化链表时，要用每个节点来比较